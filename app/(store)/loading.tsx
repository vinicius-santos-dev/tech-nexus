import Loader from "@/components/Loader";

/**
 * Loading Component
 *
 * Next.js loading component for the store group route.
 * Automatically displayed during page transitions and data fetching.
 */
function Loading() {
  return <Loader />;
}

export default Loading;
